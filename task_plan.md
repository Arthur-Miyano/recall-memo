# 继续上次记忆：实现计划

## 目标
在首页提供“继续上次记忆”入口：用户退出训练页或重新启动应用后，可以恢复未完成的真实背诵会话；同时保留“开始新一轮”入口。

## 阶段

1. **刷新诊断与状态边界** — complete
2. **建立“退出后继续”失败测试** — complete
3. **实现首页继续入口与恢复路由** — complete
4. **单测、构建与隔离浏览器 E2E** — complete
5. **清理与交付** — complete

## 完成标准
- 首页仅在存在未完成、非 mock 的记忆快照时显示“继续上次记忆”
- 点击继续不生成 fresh token，不创建新会话，并恢复原题目/阶段
- 点击“开始新一轮”仍会生成 fresh token并新建会话
- 前端单测、生产构建和隔离浏览器 E2E 全部通过

## Errors Encountered
- apply_patch 在 Windows sandbox helper 初始化异常时无法读取规划文件；规划记录改用精确写入。
- Node 直接加载 Vite 无扩展导入失败；测试改用 Vite ssrLoadModule。

## 当前阻点
- 无。
- PowerShell 变量名大小写不敏感，误用 `$home` 触发只读变量错误；未落盘，已改名。
- E2E 工具问题：初始 mock UI 也包含考核按钮，等待条件过早；改为等待真实会话题目。
