[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
# 本机临时目录 ACL 问题请用本地配置（如 PYTEST_ADDOPTS 或临时加 --basetemp）处理，不进仓库默认配置
# 覆盖率门禁：只统计生产代码（见 .coveragerc），行覆盖率低于 90% 则测试失败
addopts = --cov=. --cov-fail-under=90
