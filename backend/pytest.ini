[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
# 本机 %TEMP%\pytest-of-arthur 目录 ACL 损坏（PermissionError），改用项目内目录
# 覆盖率门禁：只统计生产代码（见 .coveragerc），行覆盖率低于 90% 则测试失败
addopts = --basetemp=.pytest-tmp --cov=. --cov-fail-under=90
