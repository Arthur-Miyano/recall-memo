# 程序员八股背诵 Agent — 项目需求与开发文档

> 版本：V1.0  
> 日期：2026-08-13  
> 状态：需求已收敛，技术栈已确认，待进入开发

---

## 第一部分：需求总览

### 1.1 项目定位

一个**模拟真实技术面试场景的八股背诵辅助 Agent**。核心目标不是题库问答机，而是让使用者在"渐进式记忆训练"和"高压面试模拟"两种模式下，既能记住八股内容，又能用自己的语言表达出来，避免机械背诵。

### 1.2 核心设计原则

| 原则 | 说明 |
|------|------|
| **反背诵检测** | 回答与标准答案逐字重合率超过 30%，系统识别为背诵痕迹，评分权重降低 |
| **题干变体化** | 同一道题每次出现，面试官的提问方式必须不同，但技术内核不变 |
| **渐进式记忆** | 从短时少量记忆开始，逐步增加记忆负荷，由用户自主控制阶段 |
| **压力模拟** | 面试模式下引入时间压力，模拟真实面试的紧张感 |
| **语音输入** | 后续迭代加入，MVP 阶段仅支持文字输入 |

### 1.3 用户画像

- 使用者：具备一定开发经验、需要背诵技术面试八股文的程序员
- 题库提供者：项目设计者（非使用者）
- 部署方式：本地单机使用，无需登录

---

## 第二部分：功能需求

### 2.1 题库管理

| 需求项 | 说明 |
|--------|------|
| 题库来源 | 由项目设计者自行提供 |
| 覆盖范围 | MVP 仅覆盖 **Python、Agent、Vue 3** 三个方向，预留未来扩展位 |
| 输入格式 | PDF（标准格式 + 自由排版），需经过文档清洗工序 |
| 题库结构 | 每道题至少包含：标准题干、标准答案、技术栈、难度、关键词、标签 |
| 追问组 | 同一知识点的 2~3 道递进题可组成追问链，设定递进顺序 |
| 语义去重 | 清洗过程中识别重合题目，标记为"疑似重复"供人工确认 |

### 2.2 题干变体（面试官口吻重述）

- 从题库抽取题目后，**不直接输出原始题干**
- 由 LM 实时生成面试官口吻的变体题干
- **约束**：修改幅度不超过 40%，技术内核和考察范围不变
- 同一道题每次出现时，提问方式应不同

### 2.3 三种模式（前后端完全独立，用户自选进入）

#### 模式一：记忆训练模式

| 项目 | 说明 |
|------|------|
| 阶段选择 | 用户自选 3 题 / 5 题 / 7 题，无强制通关门槛 |
| 抽题策略 | 基于历史表现，优先抽取从未背过的题目，其次抽历史表现差的题目（2026-08-13 需求评审确认） |
| 记忆流程 | 系统展示该阶段所有题目的标准题干 + 标准答案，供用户记忆 |
| 考核触发 | 用户主动点击"我记好了，开始考核"后，系统打乱顺序逐一提问 |
| 考核界面 | 只显示变体题干，不显示答案 |
| 半开卷支持 | 提供"提示"按钮，点击后显示该题的关键词标签（如 `["GIL", "多线程", "IO密集型"]`），不显示完整答案 |
| 反馈时机 | **即时反馈**：每题答完后立即显示结构化评分、点评、标准答案对照 |
| 题间过渡 | 评分面板关闭后，显示"下一题"按钮，用户自主控制节奏 |

#### 模式二：面试模拟模式

| 项目 | 说明 |
|------|------|
| 选题范围 | 用户进入前自选技术栈（Python / Agent / Vue 3 / 混合）和题量（3~5 题） |
| 抽题策略 | 系统智能选题（非完全随机），基于用户历史表现；连续 3 次不经补答成功则短期内排除该题；可重复但后台记录 |
| 题干展示 | 逐题展示，屏幕上永远只有当前 1 题 |
| 追问链 | 混合结构：可能是"1 个追问链（含 2 个递进小问）+ 1 个独立单题"，也可能是"3 道完全独立单题" |
| 追问执行 | 单场面试内追问链固定，按预设递进顺序执行；跨场次根据用户表现动态调整下一场题目组合 |
| 时间压力 | 每道题限时 2 分钟内必须开始作答（检测到输入即算"开始"），开始后可继续说完，不限时 |
| 超时处理 | 2 分钟到后不强制跳转，前端提示"已超时"，用户可继续答或点击"跳过" |
| 跳过处理 | 标记为失败，不消耗补答机会，直接判负，不给补答 |
| 反馈时机 | **终局复盘**：全程不透露任何单题的对错、得分、颜色标记，仅显示"已记录"和"下一题"按钮 |
| 补答机制 | 全部题目答完后，列出标记为"需补答"的题目，每道题仅允许补答 1 次 |
| 追问标识 | 若当前题属于追问链，题干下方显示小字"追问 1/2"或"追问 2/2" |

#### 模式三：回忆模式（复习已背题目，2026-08-13 需求评审新增）

| 项目 | 说明 |
|------|------|
| 定位 | 独立的第三种模式，类似艾宾浩斯复习计划，帮助用户巩固**已背过**的题目 |
| 选题范围 | 只从历史记录中抽取已出现过的题目，不引入新题 |
| 复习调度 | 基于遗忘曲线：按"距上次出现天数 + 历史得分/成功率"计算到期复习题，到期未复习的优先 |
| 记忆流程 | 同记忆训练模式：先展示题干 + 答案供回忆，用户主动触发考核，打乱顺序提问 |
| 考核与反馈 | 同记忆训练模式：半开卷提示、即时反馈、自主控制节奏 |
| 空状态处理 | 无任何历史记录时，提示用户先进入记忆训练模式 |

### 2.4 评分体系（结构化）

| 维度 | 权重 | 说明 |
|------|------|------|
| **核心考点准确性** | 50% | 是否覆盖标准答案的关键技术点 |
| **逻辑清晰度** | 30% | 答案结构是否条理分明、逻辑清晰 |
| **表达自然度** | 20% | 反背诵检测，逐字重合率 > 30% 则此项大幅扣分 |

**反背诵判定**：
- 使用 difflib.SequenceMatcher 计算用户回答与标准答案的逐字重合率
- 重合率 > 30% 判定为背诵痕迹，`表达自然度` 维度得分降至 0~5 分
- 不要求用户必须使用"在我们项目里..."等经验性表述

**评分输出**：
- 结构化 JSON，包含各维度得分、总分、是否背诵、遗漏关键点列表、定性点评

### 2.5 复盘机制（终局）

面试全部结束后，系统输出完整复盘报告：

1. **逐题对照**：用户回答 vs 标准答案，并排或上下对比展示
2. **结构化评分**：每题各维度得分（此时才展示）
3. **薄弱点分析**：由 LM 智能分析，指出遗漏考点、理解偏差、背诵痕迹、整体薄弱方向
4. **知识图谱**：粗粒度，三个主节点（Python、Agent、Vue 3），下挂子节点（具体技术概念），节点颜色区分掌握度（绿色=掌握、黄色=薄弱、灰色=未覆盖）
5. **统计看板**：近 7 天答题趋势折线图、各技术栈正确率柱状图
6. **学习建议**：推荐优先背诵的题目清单
7. **补答区域**：列出需补答题目，补答后更新分数但不覆盖原记录

### 2.6 智能助理（日历形象）

智能助理 Agent 作为中央记忆中枢，被所有其他 Agent 读写，是单一数据源。

**功能清单**：
- **日历视图**：以日历形式展示用户每天背诵的题数、成功率、失败率
- **历史追踪**：记录每道题的首次出现日期、最近出现日期、累计出现次数、连续成功次数、平均得分
- **知识掌握梳理**：
  - 擅长哪些知识点
  - 欠缺哪些知识点
  - 应该补充哪些知识点
- **进度统计**：
  - 各技术栈正确率
  - 当前还有多少面试题未背诵
  - 已覆盖题数 / 总题数
- **主动推荐**：基于"很久没出现且历史表现差"的题目，生成今日建议背诵清单

### 2.7 题库清洗（半自动）

1. **PDF 解析**：提取文本，识别题干/答案边界
2. **半自动确认**：Agent 提取后标记不确定边界的题目，由项目设计者在管理界面人工确认后再入库
3. **语义去重**：用 Embedding 计算题目相似度，> 0.85 标记为疑似重复，人工确认
4. **追问组生成**：人工勾选 2~3 道关联题，设定递进顺序
5. **题目分级**：根据答案长度和概念数量，自动标记为基础/中等/困难

---

## 第三部分：Agent 架构

### 3.1 Agent 拆分（6 个）

| Agent | 职责 | 协作关系 |
|-------|------|----------|
| **总控 Agent (Orchestrator)** | 用户对话唯一入口，解析意图，编排调用链，维护状态机，处理异常 | 调度所有其他 Agent |
| **面试官 Agent** | 题干变体生成、提问语气模拟、计时控制、接收用户回答、执行追问链 | 从策略 Agent 获取题目，将回答转发给评分 Agent |
| **策略 Agent** | 智能抽题、追问链设计、管理"连续 3 次成功排除"规则、控制重复频率 | 读取智能助理的历史数据，输出题目/追问链给面试官 |
| **评分 Agent** | 反背诵检测、内容准确性评分、逻辑清晰度评分、表达自然度评分、逐题点评生成 | 接收面试官转发的回答，输出结构化评分结果 |
| **智能助理 Agent** | 中央记忆 + 复盘分析。日历、历史记录、统计看板、知识图谱、终局复盘报告、主动推荐 | 被所有 Agent 读写，是单一数据源 |
| **题库管家 Agent** | 离线运行。PDF 解析、语义去重、题目分级、生成追问组、半自动清洗界面数据准备 | 系统初始化/更新时运行，输出结构化题库 |

### 3.2 关键设计原则

- **完全可见**：界面上必须实时展示当前活跃 Agent 名称（如"面试官 Agent 提问中..."），切换时有淡入淡出动画
- **并行场景**：用户提交回答后，评分 Agent 和智能助理 Agent 通过 `asyncio.gather` 并行工作，互不阻塞
- **状态持久化**：所有状态变更写入 SQLite，前端通过 SSE 实时读取

### 3.3 状态机

```
IDLE
├── MEMORIZE_SHOW（显示题干+答案）
│   └── MEMORIZE_QUIZ（打乱后考核）
│       └──（结束）
└── INTERVIEW_SELECT（选技术栈/题量）
    └── INTERVIEW_ASK（展示变体题干）
        └── INTERVIEW_ANSWER（等待回答，2分钟计时）
            └── INTERVIEW_SCORE（评分中，评分+助理并行）
                └── INTERVIEW_ASK（下一题）
                    └── ...
                        └── INTERVIEW_REVIEW（终局复盘）
```

---

## 第四部分：技术栈

### 4.1 前端

| 技术 | 版本/说明 |
|------|-----------|
| Vue 3 | Composition API, `<script setup>` |
| Element Plus | UI 组件库 |
| Vite | 构建工具 |
| Vue Router | 前端路由（单页应用） |
| Pinia | 状态管理 |
| ECharts | 图表库（折线图、柱状图、Graph 力导向图） |

### 4.2 后端

| 技术 | 版本/说明 |
|------|-----------|
| FastAPI | Web 框架 |
| SQLite | 本地数据库 |
| SQLAlchemy / SQLModel | ORM |
| Pydantic v2 | 数据校验与序列化 |
| python-dotenv | 环境变量管理 |
| httpx | 异步 HTTP 客户端（调用 LLM API） |

### 4.3 Agent 调度

| 技术 | 说明 |
|------|------|
| 手写状态机 | 不引入 LangGraph / CrewAI / AutoGen，完全透明可控 |
| asyncio.gather | 并行执行（如评分与写库同时发生） |
| SSE (Server-Sent Events) | 前端实时同步当前 Session 状态和活跃 Agent |

### 4.4 LLM 接入

| 技术 | 说明 |
|------|------|
| 多国产 API 统一封装 | 抽象基类 `BaseLLMClient`，下挂 `DeepSeekClient`、`KimiClient`、`ZhipuClient`、`DoubaoClient` |
| 纯远程调用 | 不部署本地模型，全部调用云端 API |
| 路由策略 | 可按 Agent 分配不同模型，或按可用性轮询 |
| 兜底机制 | 某 Provider 超时/限流时，自动切换到下一个 |

### 4.5 数据持久化

| 技术 | 说明 |
|------|------|
| SQLite | 用户历史记录、答题记录、统计表、Session 状态 |
| 本地 JSON | 清洗后的结构化题库文件 |
| 单 worker 部署 | `uvicorn --workers 1`，规避 SQLite 并发写入锁问题 |

### 4.6 部署

- 本地单机运行
- 无需用户登录
- 启动方式：`uvicorn main:app --workers 1`
- 前端通过 Vite dev server 或构建为静态文件由 FastAPI 托管

---

## 第五部分：实现目标与交付里程碑

### Phase 1：基础设施（第 1~2 周）

**目标**：搭好骨架，能跑通"读取题库 → 调用 LLM → 返回一句话"。

**关键任务**：
- 项目脚手架（backend/ + frontend/ + data/）
- 数据库 Schema 设计（questions, question_groups, sessions, records, stats）
- LLM 统一封装层（多 Provider 抽象 + 自动切换）
- 题库 JSON 格式定义
- 最小可运行交付：POST 测试接口能调用任意配置的 API 并返回结果

### Phase 2：核心引擎（第 3~5 周）

**目标**：6 个 Agent 能独立工作，总控状态机能跑通一次单题记忆模式。

**关键任务**：
- Agent 基类设计（`BaseAgent` + `run` 接口）
- 总控 Agent 状态机实现（`InterviewSession` + `SessionState` 枚举）
- 面试官 Agent（变体生成 Prompt + 追问链执行）
- 策略 Agent（智能抽题算法 + 排除规则）
- 评分 Agent（结构化评分 JSON + 反背诵检测）
- 智能助理 Agent（写库 + 统计查询接口）
- 最小可运行交付：前端选择"Python 记忆训练 3 题"→ 显示答案 → 用户记忆 → 打乱考核 → 即时评分

### Phase 3：完整模式（第 6~7 周）

**目标**：两种模式完整闭环，面试模拟终局复盘可输出。

**关键任务**：
- 记忆训练模式完整实现（3/5/7 题自选、半开卷提示、即时反馈、无通关门槛）
- 面试模拟模式完整实现（技术栈自选、题量自选、2 分钟限时、追问链、跳过处理、全程无反馈）
- 终局复盘报告生成（逐题对照、薄弱点分析、学习建议）
- 补答机制（面试结束后统一补答、每题仅 1 次）
- 最小可运行交付：完成一次 3 题面试模拟（含追问链）→ 终局复盘 → 补答 → 再次复盘

### Phase 4：前端可视化（第 8~9 周）

**目标**：前端界面完整，日历和知识图谱可用。

**关键任务**：
- 6 个核心页面实现（首页、记忆训练、面试模拟、仪表盘、设置、题库清洗）
- 智能助理仪表盘（日历组件、统计卡片、薄弱点列表、知识掌握度总览）
- 知识图谱可视化（ECharts Graph，粗粒度 3 主节点 + 子节点）
- 状态实时同步（SSE 监听 SessionState，展示当前活跃 Agent）
- 最小可运行交付：前端能完整跑通记忆模式 + 面试模式 + 仪表盘

### Phase 5：题库系统（第 10 周）

**目标**：能导入 PDF，半自动清洗入库。

**关键任务**：
- PDF 文本提取（标准格式自动切分 + 自由格式整段提取）
- 半自动清洗界面（文本块列表、编辑区、确认/跳过/去重标记）
- 语义去重（Embedding 相似度计算 + 人工确认队列）
- 追问组人工绑定
- 最小可运行交付：上传 PDF → 解析 → 人工确认 3 题 → 入库 → 能在记忆模式中被抽到

### Phase 6：打磨与扩展（持续）

**目标**：稳定运行，预留扩展。

**关键任务**：
- 多 API 切换前端配置页
- 语音输入预留接口（后续迭代）
- 边界 Case 处理（空题库、LLM 全超时、前端断连重连）
- 性能优化（大数据量抽题 SQL、图表懒加载）
- 题库扩展预留（Go、Java 等新方向，零代码改动接入）

---

## 第六部分：风险与规避

| 风险 | 影响 | 规避方案 |
|------|------|----------|
| LLM API 不稳定/限流 | 变体生成或评分中断 | 多 Provider 轮询 + 本地缓存已生成变体 |
| SQLite 并发写入 | FastAPI 多 worker 冲突 | 单 worker 启动（`--workers 1`） |
| 题库规模小导致重复抽题 | 用户体验差 | MVP 允许重复，智能助理标记熟练后自动降权 |
| 前端 SSE 断连 | 状态不同步 | 心跳检测 + 断连后主动查询当前状态 |
| PDF 自由排版解析失败 | 清洗效率低 | 半自动流程，不确定边界人工确认 |
| 追问链预设质量差 | 面试体验割裂 | 由项目设计者人工审核追问组，确保递进逻辑合理 |

---

## 附录：术语表

| 术语 | 定义 |
|------|------|
| 八股 | 技术面试中高频出现、有标准答案范式的理论题 |
| 变体题干 | 基于原始题干，由 LM 重新组织语言生成的面试官口吻提问 |
| 追问链 | 同一知识点的 2~3 道递进题，按预设顺序连续考察 |
| 重合率 | 用户回答与标准答案的逐字文本匹配比例 |
| 半开卷 | 考核时允许查看关键词提示，但不显示完整答案 |
| 终局复盘 | 面试全部结束后统一输出的完整分析报告 |
| 智能助理 | 中央记忆 Agent，负责历史记录、统计、日历、知识图谱 |
